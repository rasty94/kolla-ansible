Developer Testing Guide
=======================

How to run unit and integration tests locally:

- Install dev dependencies: `pip install -r requirements-dev.txt`
- Run unit tests: `pytest -q`
- Run coverage: `pytest --cov=kolla_ansible`

Add guidelines for writing tests, fixtures and integration test approaches.